name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

env:
  NODE_VERSION: "24"

jobs:
  typecheck:
    name: Type Check
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck

  test:
    name: Test
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm test

  build:
    name: Build (${{ matrix.platform }})
    runs-on: self-hosted
    strategy:
      matrix:
        platform: [web, ios, android]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: npx expo export --platform ${{ matrix.platform }} --output-dir dist/${{ matrix.platform }} --clear
      - uses: actions/upload-artifact@v4
        if: matrix.platform == 'web'
        with:
          name: web-build
          path: dist/web

  deploy-staging:
    name: Deploy to Staging
    needs: [typecheck, test, build]
    if: github.ref == 'refs/heads/main' && github.event_name != 'workflow_dispatch'
    runs-on: self-hosted
    environment: staging
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      # Build web (no secrets baked in — config.json is generated at runtime)
      - name: Build web for staging
        run: npx expo export --platform web --output-dir dist/web --clear
      # Docker build BEFORE GCP auth (auth@v2 can interfere with Docker Hub pulls)
      - name: Build Docker image
        run: |
          IMAGE=us-central1-docker.pkg.dev/toqui-infra/toqui-frontend/toqui-frontend
          docker build -t $IMAGE:${{ github.sha }} -t $IMAGE:latest .
      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}
      - uses: google-github-actions/setup-gcloud@v2
      - name: Push Docker image
        run: |
          IMAGE=us-central1-docker.pkg.dev/toqui-infra/toqui-frontend/toqui-frontend
          gcloud auth configure-docker us-central1-docker.pkg.dev --quiet
          docker push $IMAGE:${{ github.sha }}
          docker push $IMAGE:latest
      - name: Deploy to Cloud Run
        run: |
          IMAGE=us-central1-docker.pkg.dev/toqui-infra/toqui-frontend/toqui-frontend
          gcloud run deploy toqui-frontend \
            --image=$IMAGE:${{ github.sha }} \
            --region=us-central1 \
            --project=toqui-staging \
            --port=8080 \
            --allow-unauthenticated \
            --remove-secrets=GOOGLE_CLIENT_ID \
            --set-env-vars="EXPO_PUBLIC_API_URL=https://staging-api.toqui.travel,GOOGLE_CLIENT_ID=${{ vars.GOOGLE_CLIENT_ID }}"

  deploy-prod:
    name: Deploy to Production
    needs: [typecheck, test, build]
    if: github.event_name == 'workflow_dispatch'
    runs-on: self-hosted
    environment: production
    env:
      IMAGE: us-central1-docker.pkg.dev/toqui-infra/toqui-frontend/toqui-frontend
    steps:
      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.GCP_PROD_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_PROD_SERVICE_ACCOUNT }}
      - uses: google-github-actions/setup-gcloud@v2
      - name: Verify staging image exists
        run: |
          gcloud artifacts docker images describe $IMAGE:${{ github.sha }} \
            --project=toqui-infra || \
            (echo "ERROR: Image $IMAGE:${{ github.sha }} not found. Run staging deploy first." && exit 1)
      - name: Deploy to Cloud Run (promote staging image)
        run: |
          gcloud run deploy toqui-frontend \
            --image=$IMAGE:${{ github.sha }} \
            --region=northamerica-northeast1 \
            --project=toqui-prod \
            --port=8080 \
            --allow-unauthenticated \
            --remove-secrets=GOOGLE_CLIENT_ID \
            --set-env-vars="EXPO_PUBLIC_API_URL=https://api.toqui.travel,GOOGLE_CLIENT_ID=${{ vars.GOOGLE_CLIENT_ID }}"
