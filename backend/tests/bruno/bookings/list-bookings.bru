meta {
  name: List Bookings
  type: grpc
  seq: 2
}

grpc {
  url: {{host}}/toqui.v1.BookingService/ListBookings
  body: json
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "trip_id": "<trip-id>"
  }
}
