meta {
  name: Refresh Token
  type: grpc
  seq: 2
}

grpc {
  url: {{host}}/toqui.v1.AuthService/RefreshToken
  body: json
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "refresh_token": "<your-refresh-token>"
  }
}
